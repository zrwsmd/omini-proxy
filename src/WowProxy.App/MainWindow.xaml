<Window x:Class="WowProxy.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WowProxy.App"
        xmlns:views="clr-namespace:WowProxy.App.Views"
        xmlns:controls="clr-namespace:WowProxy.App.Controls"
        xmlns:converters="clr-namespace:WowProxy.App.Converters"
        mc:Ignorable="d"
        Title="WowProxy" Height="600" Width="1000">
    <Window.Resources>
        <converters:StatusColorConverter x:Key="StatusColorConverter" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 顶部状态栏 -->
        <Border Grid.Row="0" Background="#F5F5F5" Padding="12,8" BorderBrush="#DDD" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="当前内核: " VerticalAlignment="Center" FontWeight="Bold" />
                    <ComboBox Margin="8,0,0,0" Width="120" SelectedIndex="0">
                        <ComboBoxItem Content="sing-box" />
                    </ComboBox>
                    
                    <TextBlock Text="状态: " VerticalAlignment="Center" Margin="24,0,0,0" FontWeight="Bold" />
                    <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center" Margin="8,0,0,0">
                        <TextBlock.Foreground>
                            <Binding Path="StatusText" Converter="{StaticResource StatusColorConverter}" />
                        </TextBlock.Foreground>
                    </TextBlock>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <CheckBox Content="系统代理" IsChecked="{Binding EnableSystemProxy}" VerticalAlignment="Center" Margin="0,0,16,0" />
                    <CheckBox Content="TUN 模式" IsChecked="{Binding EnableTun}" VerticalAlignment="Center" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- 主内容区 -->
        <TabControl Grid.Row="1" Margin="12" SelectedIndex="{Binding MainTabSelectedIndex}">
            <TabItem Header="仪表盘">
                <Grid Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="200" />
                    </Grid.ColumnDefinitions>

                    <!-- 左侧：节点列表 -->
                        <Grid Grid.Column="0" Margin="0,0,12,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <!-- 订阅管理 -->
                            <Grid Grid.Row="0" Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="订阅 URL" VerticalAlignment="Center" Margin="0,0,8,0" />
                                <TextBox Grid.Column="1" Text="{Binding SubscriptionUrl, UpdateSourceTrigger=PropertyChanged}" />
                                <Button Grid.Column="2" Content="更新" Margin="8,0,0,0" Padding="12,4" Command="{Binding UpdateSubscriptionCommand}" />
                            </Grid>

                            <!-- 导入链接 -->
                            <Grid Grid.Row="1" Margin="0,0,0,8">
                                 <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="快速导入" VerticalAlignment="Center" Margin="0,0,8,0" />
                                <TextBox Grid.Column="1" Text="{Binding NodeImportText, UpdateSourceTrigger=PropertyChanged}" Height="26" />
                                <Button Grid.Column="2" Content="导入" Margin="8,0,0,0" Padding="12,4" Command="{Binding ImportLinksCommand}" />
                            </Grid>

                            <!-- 分组 Tab 栏 -->
                            <Grid Grid.Row="2" Margin="0,0,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                
                                <ScrollViewer Grid.Column="0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                                    <ListBox ItemsSource="{Binding NodeGroups}"
                                             SelectedItem="{Binding SelectedGroup, Mode=TwoWay}"
                                             BorderThickness="0,0,0,1"
                                             BorderBrush="#DDD"
                                             Background="Transparent"
                                             Padding="0">
                                        <ListBox.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal" />
                                            </ItemsPanelTemplate>
                                        </ListBox.ItemsPanel>
                                        <ListBox.ItemContainerStyle>
                                            <Style TargetType="ListBoxItem">
                                                <Setter Property="Padding" Value="12,6" />
                                                <Setter Property="Margin" Value="0,0,2,0" />
                                                <Setter Property="Background" Value="#F0F0F0" />
                                                <Setter Property="BorderBrush" Value="#CCC" />
                                                <Setter Property="BorderThickness" Value="1,1,1,0" />
                                                <Setter Property="Cursor" Value="Hand" />
                                                <Setter Property="Tag" Value="{Binding DataContext, RelativeSource={RelativeSource AncestorType=ListBox}}" />
                                                <Setter Property="ContextMenu">
                                                    <Setter.Value>
                                                        <ContextMenu>
                                                            <MenuItem Header="重命名分组..." 
                                                                      Command="{Binding PlacementTarget.Tag.RenameGroupCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" 
                                                                      CommandParameter="{Binding}" />
                                                            <Separator />
                                                            <MenuItem Header="删除分组及节点" 
                                                                      Command="{Binding PlacementTarget.Tag.DeleteGroupCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" 
                                                                      CommandParameter="{Binding}" Foreground="DarkRed" />
                                                        </ContextMenu>
                                                    </Setter.Value>
                                                </Setter>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="ListBoxItem">
                                                            <Border Background="{TemplateBinding Background}"
                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                                    Padding="{TemplateBinding Padding}"
                                                                    CornerRadius="4,4,0,0">
                                                                <ContentPresenter />
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsSelected" Value="True">
                                                                    <Setter Property="Background" Value="White" />
                                                                    <Setter Property="BorderBrush" Value="#888" />
                                                                    <Setter Property="FontWeight" Value="Bold" />
                                                                </Trigger>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="Background" Value="#E8E8E8" />
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </ListBox.ItemContainerStyle>
                                    </ListBox>
                                </ScrollViewer>
                                
                                <Button Grid.Column="1" Content="+" Width="32" Height="28" ToolTip="新建分组" VerticalAlignment="Bottom" Margin="4,0,0,1" 
                                        Command="{Binding CreateGroupCommand}" Background="#F0F0F0" BorderBrush="#CCC" BorderThickness="1" 
                                        Foreground="#666" FontSize="18" FontWeight="Bold" Cursor="Hand">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="4"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </Grid>

                            <!-- 高级设置：链式代理 (可展开面板) -->
                            <Expander Grid.Row="3" Header="🚀 高级：链式代理配置" Margin="0,0,0,8" 
                                      Background="#F9F9F9" BorderBrush="#DDD" BorderThickness="1">
                                <Grid DataContext="{Binding ChainProxy}" Margin="12,8">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="150" />
                                    </Grid.RowDefinitions>

                                    <!-- 开关与预览 -->
                                    <StackPanel Grid.Row="0" Margin="0,0,0,8">
                                        <CheckBox Content="启用链式代理 (将所有流量通过多节点接力转发)" IsChecked="{Binding EnableChainProxy}" FontWeight="Bold" Margin="0,0,0,4"/>
                                        <TextBlock Text="{Binding ChainFlowText}" FontSize="12" TextWrapping="Wrap" Foreground="#2060A0" 
                                                   Background="#F0F7FF" Padding="8,4" />
                                    </StackPanel>

                                    <!-- 添加工具栏 -->
                                    <Grid Grid.Row="1" Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <ComboBox ItemsSource="{Binding AvailableNodes}" SelectedItem="{Binding SelectedAvailableNode}" DisplayMemberPath="Name" Margin="0,0,6,0" />
                                        <Button Grid.Column="1" Content="+ 添加" Padding="12,2" Command="{Binding AddToChainCommand}" />
                                    </Grid>

                                    <!-- 链路列表及操作 -->
                                    <Grid Grid.Row="2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <DataGrid Grid.Column="0" ItemsSource="{Binding ChainNodes}" SelectedItem="{Binding SelectedChainNode, Mode=TwoWay}"
                                                  AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal" BorderThickness="1" BorderBrush="#CCC">
                                            <DataGrid.Columns>
                                                <DataGridTextColumn Header="#" Binding="{Binding HopLabel}" Width="40"/>
                                                <DataGridTextColumn Header="角色" Binding="{Binding RoleName}" Width="120"/>
                                                <DataGridTextColumn Header="节点名称" Binding="{Binding DisplayName}" Width="*"/>
                                                <DataGridTextColumn Header="节点信息" Binding="{Binding NodeInfo}" Width="200"/>
                                            </DataGrid.Columns>
                                        </DataGrid>
                                        <StackPanel Grid.Column="1" Margin="6,0,0,0">
                                            <Button Content="↑" Width="24" Height="24" Margin="0,0,0,4" Command="{Binding MoveUpCommand}" ToolTip="上移" />
                                            <Button Content="↓" Width="24" Height="24" Margin="0,0,0,4" Command="{Binding MoveDownCommand}" ToolTip="下移" />
                                            <Button Content="╳" Width="24" Height="24" Margin="0,0,0,4" Command="{Binding RemoveFromChainCommand}" ToolTip="移除" Foreground="DarkRed" />
                                            <Button Content="清" Width="24" Height="24" Command="{Binding ClearChainCommand}" ToolTip="清空列表" />
                                        </StackPanel>
                                    </Grid>
                                </Grid>
                            </Expander>

                            <!-- 节点表格 -->
                          <DataGrid Grid.Row="4"
                                    x:Name="NodeDataGrid"
                                    ItemsSource="{Binding FilteredNodes}"
                                SelectedItem="{Binding SelectedNode, Mode=TwoWay}"
                                AutoGenerateColumns="False"
                                IsReadOnly="True"
                                SelectionMode="Extended"
                                SelectionChanged="NodeDataGrid_SelectionChanged"
                                HeadersVisibility="Column"
                                GridLinesVisibility="Horizontal"
                                BorderThickness="1"
                                BorderBrush="#ABADB3">
                            <DataGrid.Resources>
                                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#808080"/>
                                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="White"/>
                                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#808080"/>
                                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="White"/>
                            </DataGrid.Resources>
                            <DataGrid.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="设为活动服务器" Command="{Binding SetActiveNodeCommand}" />
                                    <MenuItem Header="复制分享链接" Command="{Binding CopyNodeLinkCommand}" />
                                    <Separator />
                                    <MenuItem Header="更改分组..." Command="{Binding SetGroupCommand}" />
                                    <Separator />
                                    <MenuItem Header="测试真延迟 (全部)" Command="{Binding TestLatencyCommand}" />
                                    <MenuItem Header="测试速度 (全部)" Command="{Binding TestSpeedCommand}" />
                                    <Separator />
                                    <MenuItem Header="移除所选节点" Command="{Binding RemoveSelectedNodesCommand}" />
                                </ContextMenu>
                            </DataGrid.ContextMenu>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsActive}" Value="True">
                                            <Setter Property="Background" Value="#2E8B57"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding IsActive}" Value="True" />
                                                <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource Self}}" Value="True" />
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Background" Value="#2E8B57"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsActive}" Value="True">
                                            <Setter Property="Background" Value="#2E8B57"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                        </DataTrigger>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding IsActive}" Value="True" />
                                                <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource Self}}" Value="True" />
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Background" Value="#2E8B57"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.CellStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="类型" Binding="{Binding Type}" Width="80"/>
                                <DataGridTextColumn Header="别名" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="地址" Binding="{Binding Server}" Width="140"/>
                                <DataGridTextColumn Header="端口" Binding="{Binding Port}" Width="60"/>
                                <DataGridCheckBoxColumn Header="TLS" Binding="{Binding TlsEnabled, Mode=OneWay}" Width="40"/>
                                <DataGridTextColumn Header="延迟" Binding="{Binding Latency}" Width="70"/>
                                <DataGridTextColumn Header="速度" Binding="{Binding Speed}" Width="80"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>

                    <!-- 右侧：控制面板 -->
                    <Grid Grid.Column="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0">
                            <Button Content="{Binding ConnectButtonText}" Height="40" FontSize="16" FontWeight="Bold" Command="{Binding ConnectCommand}" Margin="0,0,0,12" />
                            <Button Content="测试延迟" Margin="0,0,0,8" Padding="0,6" Command="{Binding TestLatencyCommand}" />
                            <Button Content="测试速度" Margin="0,0,0,8" Padding="0,6" Command="{Binding TestSpeedCommand}" />
                            <Button Content="移除所选节点" Margin="0,0,0,8" Padding="0,6" Command="{Binding RemoveSelectedNodesCommand}" />
                        </StackPanel>
                        
                        <GroupBox Header="运行日志" Grid.Row="1" Margin="0,12,0,0">
                            <TextBox Text="{Binding LogsText, Mode=OneWay}" 
                                     IsReadOnly="True" 
                                     VerticalScrollBarVisibility="Auto" 
                                     TextWrapping="Wrap" 
                                     FontFamily="Consolas" 
                                     FontSize="11" 
                                     BorderThickness="0" />
                        </GroupBox>
                    </Grid>
                </Grid>
            </TabItem>


            <TabItem Header="抓包分析">
                <Grid DataContext="{Binding MitmCapture}" Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- Row 0: 工具栏 — 启停 / 端口 / CA -->
                    <WrapPanel Grid.Row="0" Margin="0,0,0,6" Orientation="Horizontal">
                        <TextBlock Text="端口:" VerticalAlignment="Center" Margin="0,0,4,0" />
                        <TextBox Text="{Binding MitmPortText, UpdateSourceTrigger=PropertyChanged}" Width="60" Margin="0,0,8,0" VerticalAlignment="Center" />
                        <Button Content="{Binding ToggleButtonText}" Padding="12,4" Margin="0,0,6,0" FontWeight="Bold"
                                Command="{Binding StartCommand}" />
                        <Button Content="清空" Padding="10,4" Margin="0,0,6,0" Command="{Binding ClearCommand}" />
                        <Rectangle Width="1" Fill="#CCC" Margin="4,2" />
                        <Button Content="安装 CA 证书" Padding="10,4" Margin="0,0,4,0" Command="{Binding InstallCaCommand}" />
                        <Button Content="证书目录" Padding="8,4" Margin="0,0,6,0" FontSize="11" Command="{Binding OpenCaCertCommand}" />
                        <TextBlock Text="{Binding CaStatusText}" VerticalAlignment="Center" FontSize="11" Margin="4,0,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="OrangeRed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CaInstalled}" Value="True">
                                            <Setter Property="Foreground" Value="Green" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </WrapPanel>

                    <!-- Row 1: 搜索栏 (mitmproxy 风格单行搜索) -->
                    <Border Grid.Row="1" Background="#F0F4FF" BorderBrush="#B0C4DE" BorderThickness="1" CornerRadius="3" Padding="6,4" Margin="0,0,0,6">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                                     FontSize="12" FontFamily="Consolas" VerticalAlignment="Center"
                                     ToolTip="{Binding SearchHint}" />
                            <Button Grid.Column="1" Content="搜索" Padding="12,2" FontSize="11" Margin="6,0,4,0"
                                    Command="{Binding ApplyFilterCommand}" />
                            <Button Grid.Column="2" Content="清除" Padding="10,2" FontSize="11"
                                    Command="{Binding ClearFilterCommand}" />
                            <TextBlock Grid.Column="3" Text="{Binding CountText}" VerticalAlignment="Center" 
                                       Margin="8,0,0,0" FontSize="11" Foreground="#555" />
                        </Grid>
                    </Border>

                    <!-- Row 2: 主体 — 请求列表 + 详情面板 -->
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="5" />
                            <ColumnDefinition Width="3*" />
                        </Grid.ColumnDefinitions>

                        <!-- 请求列表 -->
                        <DataGrid Grid.Column="0"
                                  ItemsSource="{Binding CapturedMessages}"
                                  SelectedItem="{Binding SelectedMessage, Mode=TwoWay}"
                                  AutoGenerateColumns="False" IsReadOnly="True"
                                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                                  BorderThickness="1" BorderBrush="#CCC" FontSize="11"
                                  CanUserSortColumns="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="时间" Binding="{Binding Timestamp, StringFormat=HH:mm:ss.fff}" Width="80" />
                                <DataGridTextColumn Header="协议" Binding="{Binding Protocol}" Width="50" />
                                <DataGridTextColumn Header="方法" Binding="{Binding Method}" Width="50" />
                                <DataGridTextColumn Header="Host / SNI" Binding="{Binding Host}" Width="160" />
                                <DataGridTextColumn Header="目的IP" Binding="{Binding RemoteAddress}" Width="110" />
                                <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="*" />
                                <DataGridTextColumn Header="状态" Binding="{Binding StatusCode}" Width="42" />
                                <DataGridTextColumn Header="ms" Binding="{Binding DurationMs}" Width="45" />
                                <DataGridTextColumn Header="Size" Binding="{Binding DisplaySize}" Width="55" />
                            </DataGrid.Columns>
                        </DataGrid>

                        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="#DDD" />

                        <!-- 详情面板 -->
                        <TabControl Grid.Column="2" Margin="4,0,0,0">
                            <TabItem Header="Request">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="5" />
                                        <RowDefinition Height="120" />
                                    </Grid.RowDefinitions>
                                    <!-- Request Headers 区域 -->
                                    <TextBox Grid.Row="0" Text="{Binding SelectedRequestHeaders, Mode=OneWay}" 
                                             IsReadOnly="True" TextWrapping="Wrap"
                                             VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                                             FontFamily="Consolas" FontSize="13" BorderThickness="0" />
                                    
                                    <!-- 可拖动分隔线 -->
                                    <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch" 
                                                  Background="#DDD" />
                                    
                                    <!-- Query Params 区域 -->
                                    <Border Grid.Row="2" Background="#F9F9F9" BorderBrush="#DDD" BorderThickness="0,1,0,0" Padding="6,4">
                                        <TextBox Text="{Binding SelectedQueryParams, Mode=OneWay}" 
                                                 IsReadOnly="True" TextWrapping="Wrap" Background="Transparent"
                                                 FontFamily="Consolas" FontSize="12" BorderThickness="0" 
                                                 VerticalScrollBarVisibility="Auto" />
                                    </Border>
                                </Grid>
                            </TabItem>
                            <TabItem Header="Request Body">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <Button Content="复制 Request Body" HorizontalAlignment="Left" Padding="8,2" Margin="4"
                                            Command="{Binding CopyRequestBodyCommand}" FontSize="11" />
                                    <TextBox Grid.Row="1" Text="{Binding SelectedRequestBody, Mode=OneWay}" 
                                             IsReadOnly="True" TextWrapping="Wrap"
                                             VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                                             FontFamily="Consolas" FontSize="13" BorderThickness="0" />
                                </Grid>
                            </TabItem>
                            <TabItem Header="Response Headers">
                                <TextBox Text="{Binding SelectedResponseHeaders, Mode=OneWay}" 
                                         IsReadOnly="True" TextWrapping="Wrap"
                                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                                         FontFamily="Consolas" FontSize="13" BorderThickness="0" />
                            </TabItem>
                            <TabItem Header="Response Body">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <Button Content="复制 Response Body" HorizontalAlignment="Left" Padding="8,2" Margin="4"
                                            Command="{Binding CopyResponseBodyCommand}" FontSize="11" />
                                    <TextBox Grid.Row="1" Text="{Binding SelectedResponseBody, Mode=OneWay}" 
                                             IsReadOnly="True" TextWrapping="Wrap"
                                             VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                                             FontFamily="Consolas" FontSize="13" BorderThickness="0" />
                                </Grid>
                            </TabItem>
                        </TabControl>
                    </Grid>

                    <!-- Row 3: 状态栏 -->
                    <Border Grid.Row="3" Background="#F5F5F5" Padding="8,4" Margin="0,6,0,0" CornerRadius="3">
                        <TextBlock Text="{Binding StatusText}" FontSize="11" TextWrapping="Wrap" />
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="全球雷达">
                <Grid DataContext="{Binding Dashboard}">
                    <controls:MapTrafficRadarControl Connections="{Binding Connections}" />
                </Grid>
            </TabItem>

            <TabItem Header="进程流量">
                <Grid DataContext="{Binding Dashboard}">
                    <DataGrid
                        ItemsSource="{Binding ProcessTraffic}"
                        AutoGenerateColumns="False"
                        IsReadOnly="True"
                        HeadersVisibility="Column"
                        GridLinesVisibility="Horizontal"
                        BorderThickness="1"
                        BorderBrush="#ABADB3">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="进程" Binding="{Binding ProcessName}" Width="150">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="ToolTip" Value="{Binding ProcessPath}" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="活跃" Binding="{Binding ActiveConnections}" Width="50"/>
                            <DataGridTextColumn Header="当前上行" Binding="{Binding CurrentUploadSpeedText}" Width="90"/>
                            <DataGridTextColumn Header="当前下行" Binding="{Binding CurrentDownloadSpeedText}" Width="90"/>
                            <DataGridTextColumn Header="累计上行" Binding="{Binding CumulativeUploadText}" Width="90"/>
                            <DataGridTextColumn Header="累计下行" Binding="{Binding CumulativeDownloadText}" Width="90"/>
                            <DataGridTextColumn Header="主要规则" Binding="{Binding MainRule}" Width="100"/>
                            <DataGridTemplateColumn Header="操作" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="查看连接" 
                                                Command="{Binding DataContext.FilterByProcessCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type DataGrid}}}" 
                                                CommandParameter="{Binding ProcessName}"
                                                Padding="12,2" 
                                                Background="Transparent"
                                                BorderBrush="#DDD"
                                                Cursor="Hand" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="连接详情">
                <Grid DataContext="{Binding Dashboard}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="200" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,12,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock VerticalAlignment="Center" Margin="0,0,8,0" Text="搜索过滤" />
                        <TextBox Grid.Column="1" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" />
                    </Grid>
                    
                    <DataGrid
                        Grid.Row="1" Grid.RowSpan="3"
                        ItemsSource="{Binding Connections}"
                        AutoGenerateColumns="False"
                        IsReadOnly="True"
                        SelectionMode="Single"
                        HeadersVisibility="Column"
                        GridLinesVisibility="Horizontal"
                        BorderThickness="1"
                        BorderBrush="#ABADB3">
                        <DataGrid.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="断开连接" Command="{Binding CloseConnectionCommand}" CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ContextMenu}}}" />
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="站点" Binding="{Binding SiteName}" Width="120"/>
                            <DataGridTextColumn Header="进程" Binding="{Binding Process}" Width="140"/>
                            <DataGridTextColumn Header="目标主机" Binding="{Binding Host}" Width="180"/>
                            <DataGridTextColumn Header="端口" Binding="{Binding DestinationPort}" Width="60"/>
                            <DataGridTextColumn Header="类型" Binding="{Binding Type}" Width="80"/>
                            <DataGridTextColumn Header="网络" Binding="{Binding Network}" Width="50"/>
                            <DataGridTextColumn Header="策略链" Binding="{Binding Chains}" Width="150"/>
                            <DataGridTextColumn Header="规则" Binding="{Binding Rule}" Width="100"/>
                            <DataGridTextColumn Header="上传速度" Binding="{Binding UploadSpeedText}" Width="90"/>
                            <DataGridTextColumn Header="下载速度" Binding="{Binding DownloadSpeedText}" Width="90"/>
                            <DataGridTextColumn Header="上传总量" Binding="{Binding UploadTotalText}" Width="90"/>
                            <DataGridTextColumn Header="下载总量" Binding="{Binding DownloadTotalText}" Width="90"/>
                            <DataGridTextColumn Header="开始时间" Binding="{Binding Start, StringFormat={}{0:HH:mm:ss}}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="设置">
                <views:SettingsView DataContext="{Binding Settings}" />
            </TabItem>
        </TabControl>

        <!-- 底部简略信息 -->
        <StatusBar Grid.Row="2">
            <StatusBarItem Content="WowProxy v0.11" />
            <Separator />
            <StatusBarItem Content="{Binding SelectedNode.Name, StringFormat='节点: {0}', TargetNullValue='未选择节点'}" />
        </StatusBar>
    </Grid>
</Window>
