<Window x:Class="WowProxy.App.Views.PromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="操作提示" Height="160" Width="320" WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="MessageText" Text="请输入内容：" TextWrapping="Wrap" Margin="0,0,0,12" FontSize="14" />
        <TextBox x:Name="InputTextBox" Grid.Row="1" Height="28" VerticalContentAlignment="Center" Padding="4,0" KeyDown="InputTextBox_KeyDown" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,12,0,0">
            <Button Content="确认" Width="70" Height="28" Margin="0,0,12,0" Click="OkButton_Click" IsDefault="True" Background="#2E8B57" Foreground="White" BorderThickness="0" Cursor="Hand" />
            <Button Content="取消" Width="70" Height="28" Click="CancelButton_Click" IsCancel="True" Background="#E0E0E0" BorderThickness="0" Cursor="Hand" />
        </StackPanel>
    </Grid>
</Window>
