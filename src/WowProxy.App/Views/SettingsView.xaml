<UserControl x:Class="WowProxy.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <GroupBox Header="基础设置" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="200" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8" Text="Mixed 端口" />
                <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" Text="{Binding MixedPortText, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="0" Grid.Column="2" VerticalAlignment="Center" Margin="24,0,8,8" Text="日志级别" />
                <ComboBox Grid.Row="0" Grid.Column="3" Margin="0,0,0,8" SelectedValuePath="Content" SelectedValue="{Binding LogLevel, UpdateSourceTrigger=PropertyChanged}">
                    <ComboBoxItem Content="info" />
                    <ComboBoxItem Content="debug" />
                    <ComboBoxItem Content="trace" />
                    <ComboBoxItem Content="warn" />
                    <ComboBoxItem Content="error" />
                </ComboBox>

                <CheckBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Content="国内直连 (GeoSite/GeoIP)" IsChecked="{Binding EnableDirectCn}" />
            </Grid>
        </GroupBox>

        <GroupBox Header="Sing-Box 内核配置" Grid.Row="1" Margin="0,12,0,0" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8" Text="sing-box 路径" />
                <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,8,8" Text="{Binding SingBoxPath, UpdateSourceTrigger=PropertyChanged}" />
                <Button Grid.Row="0" Grid.Column="2" Margin="0,0,0,8" Padding="12,4" Content="浏览" Command="{Binding BrowseSingBoxCommand}" />

                <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" Margin="0,4,0,8">
                    <CheckBox Content="启用 Clash API" VerticalAlignment="Center" IsChecked="{Binding EnableClashApi}" />
                    <TextBlock Text="端口" VerticalAlignment="Center" Margin="16,0,8,0" />
                    <TextBox Width="80" Text="{Binding ClashApiPortText, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Text="Secret" VerticalAlignment="Center" Margin="16,0,8,0" />
                    <TextBox Width="200" Text="{Binding ClashApiSecret, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
